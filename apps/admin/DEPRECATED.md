# DEPRECATED — do not develop here

This Vite SPA admin dashboard is **deprecated** (all mock data). It is not
deployed anywhere: Firebase Hosting serves only a 302 redirect to the real
admin (see `firebase.json` → `firebase/admin-redirect/`).

The real, maintained admin console lives in **one** place:

    apps/sierra-estates-realty/app/admin/

served on the Vercel deployment (see `DEPLOYMENT.md` and `CLAUDE.md`,
"Admin lives in ONE place"). Make all admin changes there.

This directory is kept only for reference and is a candidate for removal.
